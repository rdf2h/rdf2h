{
    "name": "RDF2h",
    "version": "0.0.1",
    "author": "Reto Gmür",
    "description": "Library to render RDF resources.",
    "dependencies": {
        "rdf-ext": "*",
        "mustache": "*"
    },
    "devDependencies": {
        "browserify": "latest"
    },
    "scripts": {
        "install": "mkdir -p dist && browserify -d js/rdf2h-core.js js/logger.js > dist/rdf2h.js"
    }
}
